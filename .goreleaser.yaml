project_name: qrypad
builds:
  - main: ./
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
changelog:
  use: git
  sort: asc
release:
  github:
    owner: wheelibin
    name: qrypad
